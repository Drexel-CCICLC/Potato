ArrayList<T>&Indexed<T, ArrayList<T>>[T... values] = {
  +(T other) = (values.copy(size + 1)
          .index(size + 1))
  +(Indexed<T, ?> other) = (

  )
  [(int index)] = values[index]
  size = values.size
}

val list = ArrayList(3, 2, 5) + 6
