Point[x : float, y : float] = {
  distance[Point other] => float = sqrt(
    pow(other.x, 2) +
    pow(other.y, 2)
  )
}

single Origin&Point(0, 0)

val point = Point(3, 4)
val distance = point.distance(Origin)
print(distance)
