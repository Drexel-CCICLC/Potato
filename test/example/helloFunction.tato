empty = {
}
empty()

with Internal.print

singleParameter[string value] = print(value)
singleParameter("A function with one parameter!")

doubleParameter[value0 string, value1 string] = print("Hello " + value0 + value1 + "!")
doubleParameter("Gavin", "Bromberg")

returnFunction => string = "stuff"
print(returnFunction())
